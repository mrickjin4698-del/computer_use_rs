[package]
name = "alice-computer-use-sidecar-protocol"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Sidecar wire protocol for Alice Computer Use"
publish = false

[dependencies]
alice-computer-use-core = { path = "../alice-computer-use-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
